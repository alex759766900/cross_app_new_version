
import 'package:flutter/material.dart';


/// Holds on to common decorations
var defaultShadows = [
  BoxShadow(
    color: Colors.grey.withOpacity(0.25),
    spreadRadius: 0.45,
    blurRadius: 0.5,
    offset: Offset(0, 3),
  ),
  BoxShadow(
    color: Colors.grey.withOpacity(0.25),
    spreadRadius: 0.35,
    blurRadius: 1,
    offset: Offset(-1, -1),
  ),
];
